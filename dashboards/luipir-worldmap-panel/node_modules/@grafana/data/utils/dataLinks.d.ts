import { DataLink, DataSourceInstanceSettings, Field, InterpolateFunction, LinkModel, ScopedVars, TimeRange } from '../types';
export declare const DataLinkBuiltInVars: {
    keepTime: string;
    timeRangeFrom: string;
    timeRangeTo: string;
    includeVars: string;
    seriesName: string;
    fieldName: string;
    valueTime: string;
    valueNumeric: string;
    valueText: string;
    valueRaw: string;
    valueCalc: string;
};
declare type Options = {
    onClickFn?: (options: {
        datasourceUid: string;
        query: any;
    }) => void;
    replaceVariables: InterpolateFunction;
    getDataSourceSettingsByUid: (uid: string) => DataSourceInstanceSettings | undefined;
};
export declare function mapInternalLinkToExplore(link: DataLink, scopedVars: ScopedVars, range: TimeRange, field: Field, options: Options): LinkModel<Field>;
export {};
