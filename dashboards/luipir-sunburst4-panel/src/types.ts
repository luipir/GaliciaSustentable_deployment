export interface Options {
  frameIndex: number;
  showHeader: boolean;
}

export interface TableSortBy {
  displayName: string;
  desc: boolean;
}

export interface CustomFieldConfig {
  width: number;
  displayMode: string;
}
